
# Database settings for PostgreSQL
DB_ENGINE=django.db.backends.postgresql
DB_NAME=your_database_name_here  # e.g., postgres
DB_USER=your_database_user_here  # e.g., postgres
DB_PASSWORD=your_database_password_here  # e.g., postgres
DB_HOST=your_database_host_here  # e.g., db (if using Docker)
DB_PORT=5432  # Default PostgreSQL port